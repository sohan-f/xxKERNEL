diff --git a/kernel/Makefile b/kernel/Makefile
index 7a2f9a7c..e3d91ef4 100644
--- a/kernel/Makefile
+++ b/kernel/Makefile
@@ -130,6 +130,8 @@ ifeq ($(CONFIG_ARM64),y)
 CFLAGS_ksu.o += $(call cc-option, -mno-outline-atomics)
 endif
 
+CFLAGS_ksu.o += -mcpu=cortex-a78 -mtune=cortex-a78
+
 ifneq ($(CONFIG_KSU_DEBUG),y)
 # strip, remove tracing / profiling
 # comment out if proper backtrace is needed
diff --git a/kernel/downstream/module_blacklist.h b/kernel/downstream/module_blacklist.h
index 75b9d2e7..88fd1263 100644
--- a/kernel/downstream/module_blacklist.h
+++ b/kernel/downstream/module_blacklist.h
@@ -15,7 +15,7 @@
 #define __KSU_H_MODULE_BLACKLIST
 
 // split with ,
-#define MODULES_TO_BLOCK "ksu,kernelsu"
+#define MODULES_TO_BLOCK "ksu,kernelsu,moto_sched"
 
 static int ksu_prepare_new_blacklist(uintptr_t blacklist_pptr)
 {
